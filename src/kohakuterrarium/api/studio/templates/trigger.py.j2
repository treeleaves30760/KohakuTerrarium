"""
{{ class_name }} — trigger module.
"""

from typing import Any, ClassVar

from kohakuterrarium.core.events import TriggerEvent
from kohakuterrarium.modules.trigger.base import BaseTrigger


class {{ class_name }}(BaseTrigger):
{%- if universal %}
    universal: ClassVar[bool] = True
    setup_tool_name: ClassVar[str] = {{ setup_tool_name | tojson }}
    setup_description: ClassVar[str] = {{ setup_description | tojson }}
{%- endif %}

    async def wait_for_trigger(self) -> TriggerEvent | None:
{{ wait_for_trigger_body | indent(8, True) }}
